# Runtime dependencies for Sanad.
#
# pyproject.toml is the source of truth; this file mirrors it so that Streamlit
# Community Cloud — which installs from requirements.txt — resolves the same
# versions. Keep the two in step when changing a bound.
#
# Development extras (pytest, ruff) are intentionally absent here:
#   pip install -e ".[dev]"

streamlit>=1.40,<2
requests>=2.32,<3
python-dotenv>=1.0,<2
PyMuPDF>=1.24,<2
python-docx>=1.1,<2
numpy>=1.24,<3
